name: Code Quality

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:  # Allows manual triggering

jobs:
  code-quality:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.13

#    - name: Set up Node.js
#      uses: actions/setup-node@v4
#      with:
#        node-version: 18
#        cache: 'npm'

    - name: Install Python dependencies
      run: |
        pip install bandit mypy pip-audit bandit2sarif
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

#    - name: Install Node.js dependencies
#      run: npm ci

    - name: Python security check
      run: |
        # To uncomment the following lines, ensure you have a .bandit file configured
        bandit -r . -f json -o bandit.json || true
        python bandit_json_to_sarif.py bandit.json bandit.sarif || true
        pip-audit --format=json --output=pip-audit.json || true

    - name: Upload SARIF to GitHub
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: |
          bandit.sarif

    - name: Upload raw reports as artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: security-reports
        path: |
          bandit.json
          pip-audit.json

    - name: Python type checking
      run: mypy .

#    - name: JavaScript/TypeScript linting
#      run: |
#        npm run lint || true
#        npm audit || true
