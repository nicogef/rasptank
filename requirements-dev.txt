# Pinned developer/test dependencies for reproducible dev environments
black==23.9.1
pylint==2.17.5
pytest>=8.0.0,<9.0.0
pytest-cov==4.1.0
pytest-timeout==2.2.0
pytest-bdd==8.1.0
playwright==1.42.0
pip-audit==2.6.2

# Note: After installing playwright, run:
#   .venv\Scripts\python -m playwright install
# to download browser binaries required for E2E tests.
